# dataBehaviour

This is where all the data lives. The database is structured a sungle SQLite DB. Each animal group or behvaiour are stored in their own tables, while the main table stores the observations. Other apps are made to fecth and update this DB.
